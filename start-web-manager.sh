#!/bin/bash

echo "🚀 Starting Web Docker Manager..."
echo "NOTE: Web manager files have been moved to web-manager directory"

# Change to the web-manager directory and execute the script there
cd web-manager && ./start-web-manager.sh 